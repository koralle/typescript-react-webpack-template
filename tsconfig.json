{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig.json to read more about this file */

    /* Basic Options */
    "incremental": true, // 差分コンパイル
    "target": "es5" /* Specify ECMAScript target version: 'ES3' (default), 'ES5', 'ES2015', 'ES2016', 'ES2017', 'ES2018', 'ES2019', 'ES2020', or 'ESNEXT'. */ ,
    "module": "commonjs",
    // 組み込みライブラリは以下のリストに含まれるバージョンのものが使える。
    "lib": ["dom", "ES2020"],
    "allowJs": true, // .jsと.jsxもコンパイル対象に含める
    "jsx": "react", // Reactのコードへコンパイルする
    "sourceMap": true /* Generates corresponding '.map' file. */ ,
    "outDir": "./dist",
    "removeComments": true, // コンパイル結果の出力ファイルから、コンパイル対象のファイル上のコメントを削除する。
    "importHelpers": true,

    /* Strict Type-Checking Options */
    "strict": true /* Enable all strict type-checking options. */ ,
    "noImplicitAny": true /* Raise error on expressions and declarations with an implied 'any' type. */ ,
    "strictNullChecks": false /* Enable strict null checks. */ ,
    "strictFunctionTypes": true, // 関数の型チェックを厳密にチェック
    "strictBindCallApply": true, // bind, call, applyを使用する時により厳密に型チェックする。
    "noImplicitThis": true /* Raise error on 'this' expressions with an implied 'any' type. */ ,
    "alwaysStrict": true /* Parse in strict mode and emit "use strict" for each source file. */ ,


    /* Module Resolution Options */
    "moduleResolution": "node" /* Specify module resolution strategy: 'node' (Node.js) or 'classic' (TypeScript pre-1.6). */ ,
    "esModuleInterop": true /* Enables emit interoperability between CommonJS and ES Modules via creation of namespace objects for all imports. Implies 'allowSyntheticDefaultImports'. */ ,

    /* Advanced Options */
    "skipLibCheck": true /* Skip type checking of declaration files. */ ,
    "forceConsistentCasingInFileNames": true /* Disallow inconsistently-cased references to the same file. */
  }
}